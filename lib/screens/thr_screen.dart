import 'package:flutter/material.dart';


class ThrScreen extends StatelessWidget {
  const ThrScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('',style: TextStyle(fontSize: 35,color: Colors.greenAccent),),
      ),
      
    );
  }
}